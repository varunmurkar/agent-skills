{
  "$schema": "https://opencode.ai/config.json",
  "agent": {
    "explore": {
      "model": "ollama-cloud/deepseek-v4-flash:0731",
      "prompt": "You are a fast, read-only agent for exploring codebases. Use the fff MCP tools for ALL file discovery and content search: fff_find_files to locate files by name, fff_grep to search file contents, and fff_multi_grep for multiple patterns. Do NOT use the native glob, grep, or list tools. After fff locates a file, use read to open it.",
      "permission": {
        "glob": "deny",
        "grep": "deny",
        "list": "deny",
        "fff_*": "allow"
      }
    }
  }
}
