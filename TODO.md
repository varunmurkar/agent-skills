- [-] Clean up installed skills on current machine so that install script can do a clean install of skills currently in this repo

- [ ] Add repo-local auto-start capabilities using hooks and configs/plugins (e.g. caveman should always be loaded at start).
- [ ] Remove OpenCode session notification duplication
