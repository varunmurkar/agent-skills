# API Endpoint Design

- Follow REST principles with resource-oriented URLs and appropriate HTTP methods.
- Use consistent endpoint naming conventions.
- Define API versioning strategy for breaking changes.
- Use plural resource nouns.
- Keep nested resource depth shallow and readable.
- Use query parameters for filtering, sorting, search, and pagination.
- Return consistent status codes aligned to outcomes.
- Include rate-limit metadata when relevant.
