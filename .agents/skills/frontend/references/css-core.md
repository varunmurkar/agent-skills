# CSS Core

- Follow the project's selected CSS methodology consistently.
- Prefer tokens/variables over hardcoded values.
- Keep selectors narrow and predictable.
- Extend component styles rather than deep overriding.
- Avoid expensive selectors/effects where possible.
- Prefer class-based styling over DOM-structure coupling.
